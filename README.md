# E-Commerce Data Engineering

Ingest data from products API, generates fake data of users, orders, order_items. Uses dbt for data transformation and Apache Airflow for orchestration.

## Setup
pip install -r requirements.txt