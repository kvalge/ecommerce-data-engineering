# Python ingest/load deps (Docker image + optional host venv).
# dbt and Airflow run in their own images — not listed here.
requests==2.34.2
faker==40.35.0
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
python-dotenv==1.2.2
